'use strict';
import React from 'react';
import PropTypes from 'prop-types';

const MatchingList = (props) => (
    // props.matchedUsers.map(x=>1)
    <div>
        matching list
    </div>
);

MatchingList.propTypes = {

};

export default MatchingList;
